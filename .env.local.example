# Copy this file to .env.local and fill in your Supabase project details.
# Both are safe to expose to the browser (this app only ever performs
# insert-only writes, enforced by a Postgres RLS policy on the table).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
